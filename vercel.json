{
  "version": 2,
  "framework": null,
  "buildCommand": "pnpm --filter @workspace/aerukart run build && cp -r artifacts/aerukart/dist ./dist",
  "installCommand": "pnpm install",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
